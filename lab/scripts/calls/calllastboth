#!/opt/homebrew/bin/zsh

source ~/lab/scripts/calls/sourceall

cpprepfiles -d $SCREENSHOT_DIRECTORY -t "$(cat $SCREENSHOT_TIME_FILE)" -l -c && \
cpprepfiles -d $DOWNLOAD_DIRECTORY -t "$(cat $DOWNLOAD_TIME_FILE)" && \
cpprepswift
