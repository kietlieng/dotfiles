# #promptbonsai
# #ycheckrot # check to rotate
#
# # figure out if there's a new screen shot.
# #srefnew 
#
# function zhookcall
#
#   awstokensfix
#   # dreminder
#   hbackup
#   # marktmuxsession
#
# }
#
# # check if function exists before adding. Works for shell. Doesn't work on vim external shell calls (don't know why)
# if [[  $(which add-zsh-hook | grep -i "not found") != *"not found"* ]] ; then
#   add-zsh-hook precmd zhookcall 
# fi
